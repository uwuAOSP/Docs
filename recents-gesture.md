# 最近任务上滑手势

Launcher 设置中的“Pop-Up View gesture”是一个历史遗留入口。

启用后，从应用执行最近任务手势并继续向上拖动。接近触发位置时，Launcher 会显示目标区域，并让当前任务与其他最近任务卡片产生视觉反馈。达到触发位置后松手，当前任务会以 Moment 打开。

“Pop-Up View gesture threshold”用于调整需要继续上滑的距离。数值越高，需要拖动得越远。

该功能将在后期被重新实现。

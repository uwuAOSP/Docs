# 多窗口与焦点

系统可以同时保留多个 Moment。通过应用列表、MomentArc、通知或其他入口启动新内容时，已有 Moment 不需要先关闭。

- 点按一个 Moment 会把输入焦点切换到该窗口。
- 未聚焦窗口的第一次底部操作条点按只负责激活窗口，避免误触发返回。
- 每个 Moment 分别保存自己的位置、比例、Activity 栈和折叠状态。
- 将某个 Moment 恢复到全屏或关闭时，不会同时关闭其他 Moment。
- 关闭 Moment 总开关时，当前用户的所有 Moment 都会恢复到全屏。

多个窗口重叠时，最近激活的任务会位于前方。可以先移动或折叠暂时不用的窗口，为其他应用留出空间。

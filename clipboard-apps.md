# 剪贴板应用建议

复制符合规则的链接或文本后，系统剪贴板浮层会显示目标应用的操作按钮。点按按钮会尝试用该应用打开匹配内容。

## 开启与设置

进入 **uwuSettingsExt → 智能 → 智能建议**，打开剪贴板应用建议。

在规则页面中可以：

- 开启或关闭单条规则。
- 添加和编辑自定义规则。
- 删除规则。
- 恢复默认规则。

默认规则覆盖淘宝、百度网盘、123 云盘、哔哩哔哩、抖音、拼多多、京东、小红书、微博、知乎、网易云音乐、QQ 音乐、闲鱼、支付宝、钉钉、夸克和飞书等应用。

## 使用

1. 在任意应用中复制链接或文本。
2. 内容命中已启用的规则后，剪贴板浮层显示目标应用按钮。
3. 点按按钮，用目标应用打开匹配内容。

目标应用无法直接处理匹配内容时，系统可能改为打开应用主页。浮层通常在约 6 秒后消失，用户与浮层交互会重新计算显示时间。

## 适用范围

- 目标应用必须已安装、已启用且可以启动。
- 多条规则同时匹配时，只显示第一个有效建议。
- 从目标应用自身复制内容时，不会建议再次打开同一个应用。
- 链接主要支持 HTTP、HTTPS、`dtk://` 和 `m.tb.cn`；其他匹配内容可能只能打开应用主页。
- 无效规则会被忽略，不影响其他有效规则。

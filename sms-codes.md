# SMS 验证码建议

收到普通 SMS 并识别出验证码后，系统会显示验证码浮层，提供复制和填入操作。

## 开启与设置

进入 **uwuSettingsExt → 智能 → 智能建议**，打开 SMS 验证码建议。

在规则页面中可以开启、添加、编辑、删除或重置验证码规则。系统按规则排列顺序查找，使用第一条匹配结果。

## 操作

- **复制**：把验证码写入剪贴板，不会再次弹出普通剪贴板浮层。
- **填入**：尝试把验证码提交到当前聚焦的输入框。

验证码建议不会自动超时。完成复制、成功填入、滑动关闭或由系统关闭后，浮层才会消失。

## 适用范围

- 只处理普通 SMS，不处理所有端口短信或其他消息应用的通知。
- “填入”依赖当前输入法和聚焦输入框是否接受文本。
- 填入失败时不会自动改为复制，用户仍可手动选择复制。
- 锁屏时不会显示验证码建议，也不会在解锁后补弹。
- 规则无效或短信内容未命中规则时，不会显示建议。
